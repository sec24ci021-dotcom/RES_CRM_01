# Lead Management Frontend

React + Vite frontend for Lead Management System.

Commands:

```
cd client
npm install
npm run dev
```

Environment:
- Set `VITE_API_BASE_URL` in `.env` to point to the backend (e.g. http://localhost:59904/api/v1)
